const express = require('express');
const router = express.Router();
const pool = require('../db');

router.get('/filtroAnio', async(requestAnimationFrame, res) => {
    try{
        
    }catch(error){
        console.log("Error en " + error);
    }
});