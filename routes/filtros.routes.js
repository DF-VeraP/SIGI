const express = require('express');
const router = express.Router();
const pool = require('../db');

router.get('/filtroAnio', async(req, res) => {
    try{
        
    }catch(error){
        console.log("Error en " + error);
    }
});

module.exports = router;
